1：配置app的整体的样式,配置tab页
app.json


2:首页轮播
    网络请求数据
    wx.request({
        url: 'test.php', //仅为示例，并非真实的接口地址
        请求参数
        data: {
            x: '',
            y: ''
        },
        header: {
            'content-type': 'application/json' // 默认值
        },
        请求返回的数据，res
        success (res) {
            console.log(res.data)
        }
    })

    域名配置：
        对于小程序来说，
        开发管理-->开发设置中配置合法域名
        域名要求必须是https协议的域名
        开发阶段：详情中--本地设置，勾选不校验合法域名。

    获取网络请求的数据到本地
    通过setData({})来给本地的数据赋值，可以动态的更新到页面上。将数据动态的绑定到页面上

    可以在app.js中写全局的公共的函数
    可以在对应的页面的page.js中写当前页面使用的函数。

    定义配置文件config.js 将服务器的接口信息配置到这里。

    wx.showLoading
    wx.hideLoading
    wx.showToast

    整个小程序只有一个 App 实例，是全部页面共享的。开发者可以通过 getApp 方法获取到全局唯一的 App 实例，获取App上的数据或调用开发者注册在 App 上的函数。

    页面跳转：
        1：组件跳转

//明日内容
        2：API跳转： 通过事件 和 事件对象的属性 
        当前事件对象 添加 data-xx 属性
	    e.currentTarget.dataset.xx 获取数据
	流程：带着id 参数 跳转到 detail 页面，然后再网络请求当前页面的数据。

    模板的使用，模板使用的地方需要引入模板样式

    下拉刷新的实现 +下滚动增加内容的实现。
    搜索的实现